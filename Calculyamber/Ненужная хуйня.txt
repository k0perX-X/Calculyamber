            /*
            if (!(int.TryParse(config, out tf)) || (tf < 0) || (tf > 3)) //если невозможно снять значение натсроек
            {
                Console.WriteLine("Некорректный config файл/Incorrect config file/Некоректний config файл \n" +
                    "Восстановить стандартные настройки?/Restore default settings?/Вiдновити стандартнi настройки? \n" +
                    "1 - Да/Yes/Так \n" +
                    "2 - Нет/No/Нi \n");
                while (!(int.TryParse(Console.ReadLine(), out tf)) || !((tf >= 1) && (tf <= 2))) { Console.WriteLine("Неверный формат/Wrong format/Невiрний формат"); } //ввод значения
                if (tf == 1)
                {
                    CreateConfig(0); 
                }
                else { System.Environment.Exit(0); } //выход из программы
                Console.Clear();
            }*/

